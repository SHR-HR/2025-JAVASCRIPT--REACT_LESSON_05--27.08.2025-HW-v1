# Файл .gitignore - определяет какие файлы и папки игнорировать в системе контроля версий Git

# Игнорировать логи и файлы журналов
logs           # Папка с логами
*.log          # Все файлы с расширением .log
npm-debug.log* # Файлы отладки npm
yarn-debug.log* # Файлы отладки Yarn
yarn-error.log* # Файлы ошибок Yarn
pnpm-debug.log* # Файлы отладки pnpm
lerna-debug.log* # Файлы отладки Lerna

# Игнорировать зависимости и собранные файлы
node_modules   # Папка с Node.js зависимостями (не коммитить в репозиторий)
dist           # Папка с собранными файлами для production
dist-ssr       # Папка с собранными файлами для Server-Side Rendering
*.local        # Все локальные конфигурационные файлы

# Игнорировать файлы и папки редакторов кода
.vscode/*      # Все файлы из папки .vscode (настройки VS Code)
!.vscode/extensions.json # Но НЕ игнорировать файл extensions.json в папке .vscode
.idea          # Папка с настройками JetBrains IDE (WebStorm, IntelliJ и др.)
.DS_Store      # Файл метаданных macOS
*.suo          # Файлы пользовательских настроек решения Visual Studio
*.ntvs*        # Файлы проектов Node.js Tools for Visual Studio
*.njsproj      # Файлы проектов Node.js
*.sln          # Файлы решений Visual Studio
*.sw?          # Файлы свопинга (vim, nano и другие редакторы) - .swp, .swo и т.д.

# Важные заметки:
# - Символ * означает любое количество любых символов
# - Символ ? означает один любой символ
# - Строка начинающаяся с # является комментарием
# - Восклицательный знак ! в начале строки означает инверсию (НЕ игнорировать)
# - Этот файл предотвращает попадание временных и системных файлов в репозиторий
# - Все пути указываются относительно расположения самого файла .gitignore





// Импортируем функцию defineConfig из Vite
// defineConfig - это вспомогательная функция для создания конфигурации Vite с подсказками TypeScript
import { defineConfig } from 'vite'

// Импортируем плагин React из @vitejs/plugin-react
// Этот плагин обеспечивает поддержку React в Vite, включая горячую перезагрузку (HMR) и JSX
import react from '@vitejs/plugin-react'

// Экспортируем конфигурацию Vite по умолчанию
// https://vitejs.dev/config/ - документация по конфигурации Vite
export default defineConfig({
  // Массив плагинов, которые будут использоваться в Vite
  plugins: [
    // Вызываем функцию react() для создания экземпляра плагина React
    // Плагин React предоставляет:
    // 1. Поддержку JSX (трансформация JSX в JavaScript)
    // 2. Горячую перезагрузку (Hot Module Replacement) для React компонентов
    // 3. Автоматическое извлечение стилей JSX
    // 4. Оптимизации для продакшн-сборки
    react()
  ],
  
  // Дополнительные настройки конфигурации (не показаны в исходном коде, но могут быть добавлены):
  // 
  // server: {
  //   port: 3000, // Порт development сервера
  //   open: true  // Автоматически открывать браузер при запуске
  // },
  // 
  // build: {
  //   outDir: 'dist', // Папка для собранных файлов
  //   sourcemap: true // Генерация source maps для отладки
  // },
  // 
  // css: {
  //   modules: {
  //     localsConvention: 'camelCase' // Преобразование имен CSS классов в camelCase
  //   }
  // }
})

/*
  Важные технические детали о Vite конфигурации:

  1. **Vite** - современный сборщик фронтенд-приложений с быстрым холодным стартом
  2. **Плагины** - расширяют функциональность Vite (React, Vue, Svelte и др.)
  3. **Горячая перезагрузка (HMR)** - обновление кода без перезагрузки страницы
  4. **Быстрая сборка** - использование ES модулей в разработке и Rollup для продакшена

  Что делает @vitejs/plugin-react:
  - Транспилирует JSX в обычный JavaScript с помощью Babel
  - Настраивает горячую перезагрузку для React компонентов
  - Оптимизирует бандл для продакшена (tree shaking, минификация)
  - Обрабатывает CSS внутри JSX модулей

  Преимущества данной конфигурации:
  - Минимальная и чистая конфигурация
  - Оптимальная производительность в development и production
  - Полная поддержка всех возможностей React
  - Быстрая разработка с мгновенной обратной связью
*/




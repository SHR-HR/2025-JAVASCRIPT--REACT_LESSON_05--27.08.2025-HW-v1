// Импортируем стили из CSS-модуля для компонента Footer
import styles from './Footer.module.scss';

/**
 * Компонент Footer - подвал (футер) приложения
 * Отображает информацию об авторских правах, текущий год и описание приложения
 * Имеет стилизованную градиентную рамку
 */
const Footer = () => {
  // Получаем текущий год для динамического отображения в копирайте
  const currentYear = new Date().getFullYear();

  // Возвращаем JSX разметку компонента Footer
  return (
    // Элемент footer с применением стилей из CSS-модуля
    <footer className={styles.footer}>
      {/* Контейнер для центрирования содержимого */}
      <div className={styles.container}>
        {/* Обертка для текстового содержимого */}
        <div className={styles.content}>
          {/* Основной текст футера с информацией об авторских правах */}
          <p className={styles.text}>
            {/* Символ авторского права и текущий год */}
            © {currentYear} Glass Todos. 
            {/* Текст с упоминанием технологии и эмодзи */}
            Создано с ❤️ на React + Vite
          </p>
          {/* Дополнительный текст с описанием приложения */}
          <p className={styles.subtext}>
            Современное приложение для управления задачами
          </p>
        </div>
      </div>
    </footer>
  );
};

// Экспортируем компонент Footer для использования в других частях приложения
export default Footer;





